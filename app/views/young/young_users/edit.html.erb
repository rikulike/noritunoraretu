<h2>プロフィール編集</h2>
<%= form_with model: @young_user do |f| %>

<%= f.file_field :profile_image, accept: "image/*" %>
  
  <h4>名前</h4>
  <%= f.text_field :name %>
  
  <h4>自己紹介</h4>
  <%= f.text_area :introduction %>
  
  <%= f.submit "変更を保存" %>
<% end %>